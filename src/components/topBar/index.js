import { TopBarStyled } from "./styled"

export const TopBar = () => {

    return(
        <TopBarStyled>
            <span>left items</span>
            <span>right items</span>
        </TopBarStyled>
    )
}